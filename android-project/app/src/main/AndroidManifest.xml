<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    android:versionCode="1"
    android:versionName="1.0"
    android:installLocation="auto">

    <!-- OpenGL ES 2.0 -->
    <uses-feature android:glEsVersion="0x00030002" android:required="true" />

    <!-- Headset capabilities.  Every entry here is declared as optional on
         purpose: this is one APK for both Horizon OS and Android XR, and each
         platform advertises only its own feature names.  Marking either set as
         required would make the package uninstallable on the other. -->
    <!-- Horizon OS (Meta Quest). -->
    <uses-feature android:name="android.hardware.vr.headtracking" android:required="false" android:version="1" />
    <!-- Android XR.  The version is OpenXR 1.1. -->
    <uses-feature android:name="android.software.xr.api.openxr" android:version="0x00010001" android:required="false" />
    <!-- The game is driven by 6DoF controllers.  It is not marked required so
         the package still installs on a headset whose controllers are an
         optional accessory. -->
    <uses-feature android:name="android.hardware.xr.input.controller" android:required="false" />

    <!-- Touchscreen support -->
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />

    <!-- Game controller support -->
    <uses-feature
        android:name="android.hardware.bluetooth"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.gamepad"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.usb.host"
        android:required="false" />

    <!-- External mouse input events -->
    <uses-feature
        android:name="android.hardware.type.pc"
        android:required="false" />

    <!-- Audio recording support -->
    <!-- if you want to record audio, uncomment this. -->
    <!-- <uses-permission android:name="android.permission.RECORD_AUDIO" /> -->
    <!-- <uses-feature
        android:name="android.hardware.microphone"
        android:required="false" /> -->

    <!-- Camera support -->
    <!-- if you want to record video, uncomment this. -->
    <!--
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" />
    -->

    <!-- Allow downloading to the external storage on Android 5.1 and older -->
    <!-- <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="22" /> -->

    <!-- Allow access to Bluetooth devices -->
    <!-- Currently this is just for Steam Controller support and requires setting SDL_HINT_JOYSTICK_HIDAPI_STEAM -->
    <!-- <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" /> -->
    <!-- <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" /> -->

    <!-- Allow access to the vibrator -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- Required on current Quest OS to read game assets from /sdcard/SimpsonsHitRun. -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="29" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="29" />

    <!-- Allow access to Internet -->
    <!-- if you want to connect to the network or internet, uncomment this. -->
    <!--
    <uses-permission android:name="android.permission.INTERNET" />
    -->

    <!-- Create a Java class extending SDLActivity and place it in a
         directory under app/src/main/java matching the package, e.g. app/src/main/java/com/gamemaker/game/MyGame.java

         then replace "SDLActivity" with the name of your class (e.g. "MyGame")
         in the XML below.

         An example Java class can be found in README-android.md
    -->
    <application android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="true"
        android:theme="@style/AppTheme"
        android:enableOnBackInvokedCallback="false"
        android:hardwareAccelerated="true" >
        <!-- Keep Release optimizations while allowing ADB shell simpleperf to
             sample native call stacks on a connected development headset. -->
        <profileable android:shell="true" />

        <!-- Android XR: the OpenXR runtime is loaded through this system
             library.  Declared optional so the package still installs on
             Horizon OS, where the Khronos loader finds Meta's runtime
             instead. -->
        <uses-native-library android:name="libopenxr.google.so" android:required="false" />

        <!-- Android XR: an OpenXR application must be launched into Full Space
             rather than into the windowed Home Space shell. -->
        <property
            android:name="android.window.PROPERTY_XR_ACTIVITY_START_MODE"
            android:value="XR_ACTIVITY_START_MODE_FULL_SPACE_UNMANAGED" />
        <!-- The port supports room-scale movement, so ask for the large
             boundary rather than the seated one. -->
        <property
            android:name="android.window.PROPERTY_XR_BOUNDARY_TYPE_RECOMMENDED"
            android:value="XR_BOUNDARY_TYPE_LARGE" />

        <!-- Horizon OS only; both keys are ignored by other runtimes. -->
        <meta-data android:name="com.oculus.supportedDevices" android:value="quest3|quest3s" />
        <!-- Display assets/vr_splash.png in the Quest compositor while the
             process and OpenXR session are starting. -->
        <meta-data android:name="com.oculus.ossplash" android:value="true" />

        <!-- Example of setting SDL hints from AndroidManifest.xml:
        <meta-data android:name="SDL_ENV.SDL_ANDROID_TRAP_BACK_BUTTON" android:value="0"/>
         -->

        <activity android:name="com.simpsonsHitAndRun.vr.SimpsonsActivity"
            android:label="@string/app_name"
            android:alwaysRetainTaskState="true"
            android:launchMode="singleInstance"
			android:screenOrientation="landscape"
            android:configChanges="layoutDirection|locale|grammaticalGender|fontScale|fontWeightAdjustment|orientation|uiMode|screenLayout|screenSize|smallestScreenSize|keyboard|keyboardHidden|navigation"
            android:preferMinimalPostProcessing="true"
            android:exported="true"
            >
            <meta-data android:name="com.oculus.vr.focusaware" android:value="true" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <!-- The Khronos-standard immersive marker, understood by
                     Android XR, plus Horizon OS's vendor equivalent.  Extra
                     categories on a filter never narrow what it matches, so
                     carrying both is safe on either platform. -->
                <category android:name="org.khronos.openxr.intent.category.IMMERSIVE_HMD" />
                <category android:name="com.oculus.intent.category.VR" />
            </intent-filter>
            <!-- Let Android know that we can handle some USB devices and should receive this event -->
            <intent-filter>
                <action android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED" />
            </intent-filter>
            <!-- Drop file event -->
            <!--
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="*/*" />
            </intent-filter>
            -->
        </activity>
    </application>

</manifest>
